import { Metadata } from "next";
import { ReactNode } from "react";

export const metadata: Metadata = {
    title: "Foodpoint - Connexion",
};
export default function LoginLayout({ children }: { children: ReactNode }) {
    return (
        children
    );
  }