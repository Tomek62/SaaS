import { Metadata } from "next";
import { ReactNode } from "react";

export const metadata: Metadata = {
    title: "Foodpoint - Inscription",
};
export default function SubscribeLayout({ children }: { children: ReactNode }) {
    return (
        children
    );
  }
  